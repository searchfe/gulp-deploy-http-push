import * as gulp from 'gulp';
import { httpPush } from '../src';

// const PUSH = 'http://xxxx:8210';

// gulp.src(
//     `${__dirname}/assert/**`, {
//     base: `${__dirname}/assert`,
//   },

// ).pipe(httpPush([
//   {
//     host: PUSH,
//     match: '/**/*.tpl',
//     to: '/home/work/xxx/template/', // 注意这个是指的是测试机器的路径，而非本地机器
//   },
//   {
//     host: PUSH,
//     match: '/static/**',
//     to: '/home/work/sitedata/se', // 注意这个是指的是测试机器的路径，而非本地机器
//   },
// ]));

describe('Test is comming soon', () => {
  it('No Test Yet', () => {
  // do nothing
  });
});
